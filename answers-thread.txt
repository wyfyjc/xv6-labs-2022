线程0执行put函数，计算哈希索引i = key % NBUCKET；
线程0检查哈希表的相应位置，该位置不存在键值；
线程0被暂停，切换到线程1；
线程1执行put函数，计算哈希索引i = key % NBUCKET，恰好与线程0的计算结果相同；
线程1检查哈希表的相应位置，该位置不存在键值；
线程1在该位置插入键值；
线程1被暂停，切换到线程0；
线程0在该位置插入键值，于是线程0插入的键值被覆盖丢失了。